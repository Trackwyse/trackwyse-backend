mutation DraftOrderComplete($id: ID!) {
  draftOrderComplete(id: $id) {
    order {
      id
      created
      updatedAt
      status
      statusDisplay
    }
    errors {
      message
    }
  }
}
