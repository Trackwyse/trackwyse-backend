query Products($first: Int) {
  products(first: $first) {
    edges {
      node {
        id
        name
        description
        channel
        defaultVariant {
          id
        }
      }
    }
  }
}
